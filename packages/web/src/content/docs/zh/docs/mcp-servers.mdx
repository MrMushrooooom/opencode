---
title: MCP 服务器
description: 添加本地和远程 MCP 工具
---

你可以使用 _Model Context Protocol_（MCP）向 opencode 添加外部工具。opencode 支持两种类型：

- 本地服务器
- 远程服务器

一旦添加，MCP 工具会自动与内置工具一起提供给 LLM 使用。

---

## 配置

你可以在 opencode 配置的 `mcp` 部分定义 MCP 服务器。

### 本地服务器

在 MCP 对象中使用 `"type": "local"` 添加本地 MCP 服务器。可以添加多个 MCP 服务器。每个服务器的键名可以是任意名称。

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "my-local-mcp-server": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command"],
      "enabled": true,
      "environment": {
        "MY_ENV_VAR": "my_env_var_value"
      }
    },
    "my-different-local-mcp-server": {
      "type": "local",
      "command": ["bun", "x", "my-other-mcp-command"],
      "enabled": true
    }
  }
}
```

你也可以通过将 `enabled` 设为 `false` 来禁用服务器。这在你想临时禁用服务器而不从配置中删除它时很有用。

### 远程服务器

在 `mcp` 下使用 `"type": "remote"` 添加远程 MCP 服务器。

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "my-remote-mcp": {
      "type": "remote",
      "url": "https://my-mcp-server.com",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer MY_API_KEY"
      }
    }
  }
}
```

本地和远程服务器可以在同一个 `mcp` 配置对象中一起使用。

```json title="opencode.json"
{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "my-local-mcp-server": {
      "type": "local",
      "command": ["bun", "x", "my-mcp-command"],
      "enabled": true,
      "environment": {
        "MY_ENV_VAR": "my_env_var_value"
      }
    },
    "my-remote-mcp": {
      "type": "remote",
      "url": "https://my-mcp-server.com",
      "enabled": true,
      "headers": {
        "Authorization": "Bearer MY_API_KEY"
      }
    }
  }
}
```
