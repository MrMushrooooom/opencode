---
title: 权限控制
description: 控制智能体在你的代码库中可以执行的操作
---

默认情况下，opencode **允许所有操作**，无需明确批准。

权限系统提供精细控制，可以限制 AI 智能体在代码库中能执行的操作，允许你为敏感操作（如文件编辑、bash 命令等）配置明确的批准要求。

---

## 配置

权限在 `opencode.json` 文件的 `permission` 键下进行配置。以下是可用的选项。

---

### 编辑

使用 `permission.edit` 键控制文件编辑操作是否需要用户批准。

- `"ask"` - 编辑文件前提示用户批准
- `"allow"` - 允许所有文件编辑操作，无需批准

```json title="opencode.json" {4}
{
  "$schema": "https://opencode.ai/config.json",
  "permission": {
    "edit": "ask"
  }
}
```

---

### bash命令

控制 bash 命令是否需要用户批准。

:::提示
你可以指定哪些命令无需批准即可运行。
:::

这既可以全局配置，也能通过特定模式配置。将此设置为 `"ask"` 是最严格的模式，要求所有 bash 命令都需要批准。

例如：

- **要求所有命令都需要批准**

  ```json title="opencode.json"
  {
    "$schema": "https://opencode.ai/config.json",
    "permission": {
      "bash": "ask"
    }
  }
  ```

- **批准特定命令**

  ```json title="opencode.json"
  {
    "$schema": "https://opencode.ai/config.json",
    "permission": {
      "bash": {
        "git status": "allow",
        "git diff": "allow",
        "npm run build": "allow",
        "ls": "allow",
        "pwd": "allow"
      }
    }
  }
  ```

- **使用通配符模式限制特定命令**

  ```json title="opencode.json"
  {
    "$schema": "https://opencode.ai/config.json",
    "permission": {
      "bash": {
        "git push": "ask",
        "*": "allow"
      }
    }
  }
  ```

  此配置默认允许所有命令（`"*": "allow"`），但对 `git push` 命令需要用户批准。
