---
title: GitHub
description: 在 GitHub 问题和摘取请求中使用 opencode
---

opencode 与你的 GitHub 工作流程集成。在评论中标注 `/opencode` 或 `/oc`，opencode 将在你的 GitHub Actions 运行器中执行任务。

---

## 功能

- **分类问题**：让 opencode 查看问题并向你解释。
- **修复和实现**：让 opencode 修复问题或实现功能。它将在新分支中开展工作并提交包含所有更改的 PR。
- **安全**：opencode 在你的 GitHub 运行器内运行。

---

## 安装

在 GitHub 仓库中的项目中运行以下命令：

```bash
opencode github install
```

这将指导你安装 GitHub 应用、创建工作流程和设置密钥。

---

### 手动设置

或者你可以手动设置。

1. **安装 GitHub 应用**

   前往 [**github.com/apps/opencode-agent**](https://github.com/apps/opencode-agent)。确保它安装在目标仓库上。

2. **添加工作流程**

   将以下工作流程文件添加到仓库中的 `.github/workflows/opencode.yml`。确保在 `env` 中设置适当的 `model` 和必需的 API 密钥。

   ```yml title=".github/workflows/opencode.yml" {24,26}
   name: opencode

   on:
     issue_comment:
       types: [created]

   jobs:
     opencode:
       if: |
         contains(github.event.comment.body, '/oc') ||
         contains(github.event.comment.body, '/opencode')
       runs-on: ubuntu-latest
       permissions:
         id-token: write
       steps:
         - name: Checkout repository
           uses: actions/checkout@v4
           with:
             fetch-depth: 1

         - name: Run opencode
           uses: sst/opencode/github@latest
           env:
             ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
           with:
             model: anthropic/claude-sonnet-4-20250514
             # share: true
   ```

3. **在密钥中存储 API 密钥**

   在你的组织或项目**设置**中，展开左侧的**Secrets and variables**并选择**Actions**。然后添加必需的 API 密钥。

---

## 配置

- `model`：opencode 使用的模型。采用 `provider/model` 格式。这是**必需项**。
- `share`：分享会话。对于公共仓库，会话默认是共享的。

---

## 示例

以下是一些如何在 GitHub 中使用 opencode 的示例。

- **解释问题**

  在 GitHub 问题中添加如下评论。

  ```
  /opencode explain this issue
  ```

  opencode 将读取整个线程，包括所有评论，并回复清晰的解释。

- **修复问题**

  在 GitHub 问题中输入：

  ```
  /opencode fix this
  ```

  opencode 将会创建一个新分支，实施更改，并提交一个包含更改的 PR。

- 审查 PR 并进行更改

  在 GitHub PR 上留下以下评论。

  ```
  Delete the attachment from S3 when the note is removed /oc
  ```

  opencode 将实现请求的更改并将其提交到同一个 PR。
