---
title: Test Document 01
description: A comprehensive test document for testing AI translation workflow with various content types.
---

# Test Document 01 - AI Translation Testing

Welcome to the first test document! This document is designed to test the AI translation workflow with various content types and formatting.

> **Note**: This is a test document created specifically for testing the auto-translate workflow functionality.

---

## Getting Started

To begin testing with this document, you'll need to understand the basic structure:

```bash title="test-command.sh"
#!/bin/bash
echo "Hello from test document 01"
echo "This is a test script for translation"
```

---

## Content Types

### Code Examples

Here are some code examples in different languages:

```python title="test.py"
def hello_world():
    """Simple hello world function"""
    print("Hello, World!")
    return "success"

# Test the function
result = hello_world()
print(f"Result: {result}")
```

```javascript title="test.js"
// JavaScript example
function greet(name) {
    return `Hello, ${name}!`;
}

const message = greet("Developer");
console.log(message);
```

### Configuration Examples

Sample configuration files for testing:

```json title="test-config.json"
{
  "name": "test-document-01",
  "version": "1.0.0",
  "description": "Test document for AI translation",
  "features": ["markdown", "code-blocks", "tables"],
  "enabled": true
}
```

---

## Feature Comparison

| Feature | Status | Priority | Notes |
|---------|--------|----------|-------|
| Markdown Support | ✅ | High | Full markdown syntax support |
| Code Blocks | ✅ | High | Syntax highlighting available |
| Tables | ✅ | Medium | Responsive table design |
| Links | ✅ | Low | Internal and external links |
| Images | ⚠️ | Medium | Limited image support |

---

## Advanced Features

### Nested Lists

1. **First Level Item**
   - Second level bullet
   - Another second level item
     - Third level nested
     - More third level content

2. **Another First Level**
   - Different content
   - More examples

### Code with Comments

```bash title="complex-test.sh"
#!/bin/bash

# This is a complex test script
# It includes multiple features

# Function to test translation
test_translation() {
    local input="$1"
    local expected="$2"
    
    echo "Testing: $input"
    echo "Expected: $expected"
    
    # Simulate translation process
    if [[ "$input" == "$expected" ]]; then
        echo "✅ Test passed"
        return 0
    else
        echo "❌ Test failed"
        return 1
    fi
}

# Run tests
test_translation "Hello" "Hello"
test_translation "World" "World"
```

---

## Conclusion

This test document includes:
- **Markdown formatting** with headers and sections
- **Code blocks** in multiple programming languages
- **Tables** with structured data
- **Lists** with nested items
- **Configuration examples** in JSON format
- **Bash scripts** with comments

Perfect for testing AI translation quality and format preservation!
